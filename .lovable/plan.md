

## Plan: 5 Temporality Modes (revised — styling note)

No structural changes from the approved plan. Single cosmetic clarification:

**Selector button active state**: Keep `bg-system-accent text-white` (orange). As you noted, it visually distinguishes this as a **global transport-level control** rather than a per-track toggle like Chaos/Evolve (`bg-idm-ink`). This is the correct semantic distinction — temporality mode affects the entire sequencer, not individual tracks.

Inactive buttons remain `bg-black/5 text-idm-muted` with hover state.

All other details from the approved plan remain unchanged. Ready to implement.

